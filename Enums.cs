namespace DataFactory
{
    public enum MinuteRounding { None, Hour, HalfHour, QuarterHour};
    public enum SecondRounding { None, Second, HalfSecond };
    public enum Gender { Male, Female, Either, Unknown };
}
